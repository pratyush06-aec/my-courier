# Lambda runtime dependencies (packaged by SAM build)
# Note: dotenv is NOT included — it's only for local dev.
# Note: pytest is NOT included — it's only for testing.
boto3>=1.34.0
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.1.0
google-auth>=2.22.0
twilio>=8.0.0
requests>=2.31.0
pydantic>=2.4.0
